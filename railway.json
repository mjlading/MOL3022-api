{
    "build": {
        "env": {
            "PYTHON_VERSION": "3.10"
        },
        "buildCommand": "pip install -r requirements.txt",
        "startCommand": "uvicorn api.main:app --host 0.0.0.0 --port 8000"
    }
}